# 23553000 `CAD_B11` Follow-up — 2026-04-11

## Problem

On `/Users/idant/Downloads/23553000_B_FULL_SCAN 1.ply`, `CAD_B11` was still shipping as:
- `122.39°`
- `FAIL`

Expected family value:
- `120°`

Unlike `CAD_B6` and `CAD_B14`, this did not look like a bad global assignment. The bend was already using:
- `assignment_source = CAD_LOCAL_NEIGHBORHOOD`
- `measurement_method = surface_classification`

## What was checked

The local wing-cluster assignments around:
- `CAD_B10`
- `CAD_B11`
- `CAD_B15`

showed that `CAD_B11` was not simply losing a better local candidate to a neighboring bend. The one-to-one assignment was internally consistent:
- `CAD_B10` took its own near-center candidate
- `CAD_B15` took its own near-center candidate
- `CAD_B11` took its own near-center candidate

So the next lever was local candidate generation, not reassignment.

## Direct probe result

Recovered-face `surface_classification` probes on the aligned full-scan artifacts showed:
- the default local result stayed around `122.39°`
- smaller radii with `allow_line_locality = false` improved `CAD_B11`
- the best observed local result was about `121.35°`

That is not a full fix to `120°`, but it is materially better and moves the bend from `FAIL` into the warning band.

## Fix

Code:
- `/Users/idant/82Labs/Sherman QC/Full Project-qc-clean/backend/bend_detector.py`

Changes:
- keep the existing selected short-obtuse retry path
- add a second-tier retry for selected short-obtuse bends when:
  - angle gap is still `> 2.0°`
  - side balance is still weak (`< 0.5`)
- include smaller retry radii:
  - `22 mm`
  - `18 mm`
  - `14 mm`

This remains narrow:
- only selected-baseline short obtuse bends
- only after a successful but still materially off local result
- only as a candidate improvement path

## Result

Before:
- `/Users/idant/82Labs/Sherman QC/Full Project-qc-clean/output/23553000_case_debug_20260411_b14c.json`
  - `CAD_B11 = 122.39°`, `FAIL`

After:
- `/Users/idant/82Labs/Sherman QC/Full Project-qc-clean/output/23553000_case_debug_20260411_b11b.json`
  - `CAD_B11 = 121.76°`, `WARNING`

Nearby wing bends stayed coherent:
- `CAD_B6 = 119.64°`, `PASS`
- `CAD_B7 = 121.0°`, `WARNING`
- `CAD_B9 = 120.61°`, `PASS`
- `CAD_B10 = 119.88°`, `PASS`
- `CAD_B14 = 120.45°`, `PASS`
- `CAD_B15 = 119.09°`, `PASS`

## Remaining gap

`CAD_B11` is improved but not finished.

Current status:
- still not at the actual `120°`
- now narrow enough that the next fix should target the local surface candidate itself, not another broad retry rule
