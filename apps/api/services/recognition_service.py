from __future__ import annotations

from apps.api.legacy_context import legacy

recognize_part = legacy.recognize_part
get_recognition_status = legacy.get_recognition_status
compare_embeddings = legacy.compare_embeddings
reindex_all_parts = legacy.reindex_all_parts
