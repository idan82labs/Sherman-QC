# 23553000 wing bend ID map

## Source set
- Drawing: `/Users/idant/Downloads/שרטוט החלק 9.PDF`
- CAD: `/Users/idant/Downloads/23553000_B.stp`
- Current shipped full-case output: `/Users/idant/82Labs/Sherman QC/Full Project-qc-clean/output/guard_pack_20260411/23553000_full.json`

## Drawing truth
Page 2 `SECTION B-B` shows the wing family at nominal `120°`.
The drawing does not provide CAD bend IDs, so the mapping below is by CAD line locality and fitted face normals, not by an explicit drawing-native bend label.

## Structural bend classes in the wing family
The current shipped CAD/scan mapping resolves the seven `120°` bends into four structural classes.

### 1. Crown/apex fold between the two upper slanted wing faces
- front section: `CAD_B6`
- rear section: `CAD_B10`
- face signature:
  - one face normal near `[+0.5, 0, +0.866]`
  - one face normal near `[-0.5, 0, +0.866]`
  - bisector near `[0, 0, +1]`
- interpretation:
  - this is the top crown fold visible in `SECTION B-B`
- current results:
  - `CAD_B6 = 119.64° PASS`
  - `CAD_B10 = 119.67° PASS`

### 2. Left wall-to-upper-wing fold
- front section: `CAD_B9`
- rear section: `CAD_B11`
- face signature:
  - one face normal near `[-1, 0, 0]` or `[+1, 0, 0]` depending on side convention
  - paired with a slanted upper-wing normal near `[-0.5, 0, +0.866]`
- interpretation:
  - this is a wall/web to upper-wing transition, not the crown/apex fold
- current results:
  - `CAD_B9 = 120.61° PASS`
  - `CAD_B11 = 122.39° FAIL`

### 3. Right wall-to-upper-wing fold
- front section: `CAD_B7`
- rear section: `CAD_B15`
- face signature:
  - one face normal near vertical web orientation
  - paired with a slanted upper-wing normal near `[+0.5, 0, +0.866]` or mirrored lower sign in the front section
- interpretation:
  - mirrored wall/web to wing transition on the opposite side
- current results:
  - `CAD_B7 = 121.0° WARNING`
  - `CAD_B15 = 119.09° PASS`

### 4. Outer return / ear-side fold
- rear section only in the current selected cluster: `CAD_B14`
- face signature:
  - normals near `[+0.5, 0, +0.866]` and `[+0.5, 0, -0.866]`
  - bisector near `[+1, 0, 0]`
- interpretation:
  - outer return-side ear fold adjacent to the wing profile, not the crown/apex fold
- current result:
  - `CAD_B14 = 117.87° FAIL`

## Practical conclusion for B11
`CAD_B11` is not the same structural bend as `CAD_B10`.
It is the rear-section wall-to-upper-wing fold.
Its proper comparator is:
- primarily `CAD_B9` as the same wall-to-wing class in the earlier repeated section
- secondarily `CAD_B15` as the opposite-side wall-to-wing fold in the same rear cluster

That matters because forcing `CAD_B11` to match `CAD_B10` was the wrong mental model.
The right question is whether the rear left wall-to-wing fold is actually steeper than:
- the earlier repeated left wall-to-wing fold (`CAD_B9 = 120.61°`)
- the rear opposite-side wall-to-wing fold (`CAD_B15 = 119.09°`)

Current evidence says yes, modestly:
- `CAD_B11 = 122.39°`
- explicit legacy-face override made it worse
- profile-section made it worse
- tighter locality only moved it slightly
- 2D and 3D ROI overlays did not show an obviously wrong face pairing

## Current disposition
- Keep `CAD_B11` as a structurally correct assignment to the rear left wall-to-wing fold.
- Keep it as `WARNING`, not `PASS`.
- Do not keep treating it as a bad surrogate for the crown/apex fold.

## Next valid truth upgrade
If this bend is to be reclassified, the next evidence needed is not another locality heuristic.
It is one of:
1. external inspection / first-article truth for the rear left wall-to-wing fold
2. a drawing-to-CAD face-pair map precise enough to label the rear repeated section faces explicitly
