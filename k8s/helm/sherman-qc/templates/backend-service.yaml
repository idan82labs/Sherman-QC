apiVersion: v1
kind: Service
metadata:
  name: {{ include "sherman-qc.fullname" . }}-backend
  labels:
    {{- include "sherman-qc.backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sherman-qc.backend.selectorLabels" . | nindent 4 }}
